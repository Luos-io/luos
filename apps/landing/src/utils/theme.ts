import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    background: {
      default: '#323334',
      paper: '#323334',
    },
    primary: {
      main: '#bd99ff',
      dark: '#a370ff',
      light: '#d7c2ff',
    },
  },
});

export default theme;
